export const Settings = {
    touchEnabled: false as boolean,
    mouseEnabled: true as boolean,
    shadows: true as boolean,
    debug: true as boolean,

    // For character model debugging
    closeView: false as boolean,
}
